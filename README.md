# SmartWareHouse
Smart Warehouse Slot Allocation System Spring Boot application that manages product storage using priority-based slot allocation. Products are added via REST APIs and assigned warehouse slots dynamically based on demand frequency. Uses Spring Data JPA with H2 database and a simple HTML/JS frontend.
